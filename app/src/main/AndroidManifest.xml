<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jwatral.biggerbiceps6" >

    <application
        android:name=".greendaoexample.DaoExampleApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:screenOrientation="portrait"
        android:theme="@style/AppTheme" >
        <activity android:name=".greendaoexample.ui.views.CalendarView" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".greendaoexample.ui.activity.EditBoxActivity" />

        <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

        <activity
            android:name=".greendaoexample.ui.activity.Main"
            android:label="@string/title_activity_main" >
        </activity>
    </application>

</manifest>
